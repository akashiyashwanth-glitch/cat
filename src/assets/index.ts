/**
 * Assets bucket.
 *
 * Bundled images (logo, empty-state illustrations) will live in this folder.
 * Static PNGs are already present under `assets/` at the repo root; this
 * module is a placeholder so the folder structure is wired and typed.
 */
export {};